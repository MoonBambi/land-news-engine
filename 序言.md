序言

在乡村振兴与数字治理持续推进的背景下，农村土地流转相关信息的传播速度明显加快。政策文件、新闻报道与行业解读呈现出多平台并发、更新频率高、内容结构不统一等特征。面对持续增长的信息规模，传统人工检索与筛选方式逐渐暴露出效率不足和覆盖不全等问题，难以满足当前研究与实践对实时性、系统性和深度分析的需求。

基于这一问题，本文围绕农村土地流转新闻构建了“采集-清洗-分析-存储”的数据处理链路。核心技术包括：多来源并发采集、规则清洗与去重、Qwen 情感与关键词分析，以及 Jieba 词频统计和 MySQL 批量入库。

在此基础上，课题结合配套仓库实现可视化与交互应用，支持看板展示、资料检索和问答分析，形成“数据生产端 + 数据应用端”的协同体系。


1.1 设计背景
针对于农村土地流转新闻数据信息处置这一实际领域，国内外近年来均已展开较多探索。境外来看。部分发达经济体在推进农业数字治理过程中，通常会引入较成熟的数据采集与文本分析体系，对政策动态、市场信息与舆情变化进行持续跟踪，并在决策支持环节实现较高效率的数据利用。转向国内环境，随着乡村振兴战略深入推进，相关部门与研究机构也在加快数据化转型步伐，围绕农业政策信息治理、舆情监测与趋势研判开展了一系列实践工作。但总体而言，当前仍存在技术落地深度不足、数据标准不统一、跨来源信息整合难度大等现实问题。另一方面，无论海内外场景，都不同程度面临原始数据噪声高、信息孤岛明显、数据价值转化率不足等共性挑战。

论文目录：基于 Python 的农村土地流转新闻文本采集与分析系统的设计与实现

摘 要

Abstract

第 1 章 绪论

1.1 研究背景与意义

在乡村振兴和数字治理不断推进的过程中，农村土地流转相关信息的产生与传播都明显提速。政策发布、媒体报道和地方解读往往分散在不同网站，发布时间密集，页面结构也并不统一。若仍主要依赖人工检索、复制和整理，不仅耗时，而且容易漏掉关键线索[1]。已有研究已经开始关注农业政策文本的数据化处理，但多数工作更偏向单一环节，例如只做采集、只做分词，或只做结果展示，真正把“采集、清洗、分析、存储、交互”连成一体的实践并不多见。基于这一现实需求，本文尝试搭建一套面向农村土地流转新闻的完整处理系统，希望为政策解读、舆情观察和趋势研判提供一条可复用的技术路径。

1.2 国内外研究现状与发展趋势

农业新闻与政策文本的数据化处理并不是全新的议题，国内外都已积累了一定研究基础。国外一些相关工作更强调持续监测与数据支撑决策，技术链条相对完整；国内研究则在 Scrapy 采集、Jieba 分词、情感词典分析等方面已有较多尝试[5]。不过，从目前公开成果看，跨来源整合、工程化落地和交互式应用仍是薄弱环节。尤其是把大模型能力真正用于农业政策文本分析，并进一步延伸到 Text-to-SQL 问答场景，在国内仍处于探索期[2][3][21]。随着 Qwen 等模型逐步稳定，以及 Electron 这类跨平台框架不断成熟，把大模型分析能力嵌入桌面应用，已经具备较强的可实现性。

1.3 研究内容与目标

本文的研究目标，是把农村土地流转新闻从“网上分散的信息”转化为“可持续更新、可查询、可分析的数据资产”。围绕这一目标，全文主要开展五方面工作：①基于 Scrapy 构建三站点新闻采集模块，并补充重试、限速和去重等机制；②基于 lxml 与多策略 XPath 完成 HTML 清洗和字段提取；③调用 Qwen API 完成情感评分与关键词提取，同时借助 Jieba 统计高频词；④将分析结果去重合并后批量写入 MySQL；⑤在应用端使用 Electron 与 Vue 3 实现看板展示、资料检索、智能问答、终端管理和系统配置等功能。

1.4 章节安排

本文共七章：第 1 章阐述研究背景、现状、内容与目标；第 2 章介绍系统所涉核心技术；第 3 章进行需求分析与总体架构设计；第 4 章详述数据生产端的采集、清洗、分析与持久化实现；第 5 章详述数据应用端的看板、问答与通用业务模块实现；第 6 章通过场景化测试用例验证系统；第 7 章总结工作并展望未来改进方向。

第 2 章 系统开发技术与理论基础
2.1 数据采集与处理技术基础

2.1.1 爬虫框架 Scrapy

Scrapy 是建立在 Twisted 异步框架之上的开源爬虫工具[23]，在新闻采集、数据抓取等场景中应用广泛。其核心由引擎、调度器、下载器、爬虫和管道五个部分构成，请求与响应在这些组件之间按既定顺序流转，从而实现异步、非阻塞的数据抓取。同时，Scrapy 还提供中间件扩展机制，便于在请求头、重试、代理和日志等环节按需插入自定义逻辑。其总体结构如图 2-1 所示。

图 2-1 Scrapy 五大组件架构图
【待插入：Scrapy Engine/Scheduler/Downloader/Spider/Pipeline 五组件的数据流架构图】

在本文的实现中，针对农业农村信息网、农业农村部和人民日报/中国日报三类数据源分别编写了专用 Spider。三者都继承自 `scrapy.Spider`，但会根据站点接口差异配置不同的请求参数与解析策略。整体回调链保持一致，即 `start_requests()` 负责组织搜索请求，`parse_list()` 负责从列表结果中提取详情页链接，`parse_detail()` 负责抓取详情页 HTML 并输出包含 URL、正文和来源等字段的记录。这样的分层写法虽然朴素，但在后续调试和补站点规则时比较稳定，不容易把不同职责混在一起。

在运行参数方面，各 Spider 统一通过 `custom_settings` 设定下载间隔、并发上限、重试次数与超时时间。例如，`DOWNLOAD_DELAY = 1.0` 用于控制请求节奏，`CONCURRENT_REQUESTS = 16` 用于平衡抓取速度与本地资源消耗，`RETRY_TIMES = 2` 和 `DOWNLOAD_TIMEOUT = 10` 则负责处理网络波动带来的失败请求。针对部分站点对会话和请求头较为敏感的情况，程序在请求中透传 Cookie 与 User-Agent，以降低被直接拒绝的概率。此外，`parse_list()` 中维护 `_seen_urls` 集合，对已发现的详情页链接先做一轮内存级去重，避免同一篇文章被重复抓取。

在 Item 处理层面，本文在 pipelines.py 中定义了 LandNewsPipeline 类是数据处理入口，负责接收 Spider 产出的 Item 对象并执行后续的清洗与持久化协调。同时，在 middlewares.py 中预留了 RandomUserAgentMiddleware 与 ProxyMiddleware 等中间件扩展点，为后续引入随机 UA 轮换、代理 IP 池等功能提供标准化接入接口。Scrapy 内置的 Feed Exports 机制可以直出 JSON——笔者在实际使用中发现，这个设计比预先设想的更方便，省去了额外写序列化代码的麻烦。

2.1.2 并发采集与容错技术基础

网络爬虫的实际运行环境中，请求延迟与失败不可避免。单一顺序请求模式下。大量时间消耗在 I/O 等待上，整体采集效率低下；同时，网络波动、服务器限流、页面结构变化等因素均可能导致个别请求失败。因此需引入并发执行与多层容错机制。

并发采集与容错机制层次如图 2-2 所示。

图 2-2 并发采集与容错机制层次图
【待插入：Scrapy 异步事件循环 + RetryMiddleware + try/except + DOWNLOAD_DELAY 四层容错结构图】

在并发采集方面，Scrapy 依赖 Twisted 的事件循环完成异步调度。请求进入调度器后并不会按“发一个、等一个”的串行方式执行，而是由下载器并发发起，待响应到达后再回调对应的解析函数。结合 `CONCURRENT_REQUESTS = 16` 的配置，系统可以在较低资源开销下维持稳定的抓取吞吐。到了 LLM 分析阶段，并发模型又切换为 `asyncio` 协程：系统以 `Semaphore` 控制最大并发数，以 `batch_size = 50` 为单位批量提交任务，核心目的不是盲目追求高并发，而是尽量把网络等待时间叠起来，减少整批分析的总耗时。

容错机制贯穿 Scrapy 架构多个层面，其工程实现将在第 4 章结合图 4-2 更深入地展开说明。

Downloader Middleware 层面：Scrapy 内置 RetryMiddleware 根据 RETRY_TIMES = 2 自动对 HTTP 5xx、连接超时等异常重试。DOWNLOAD_TIMEOUT = 10 秒控制全局超时。Spider 回调层面：parse_list() 与 parse_detail() 以 try/except 包裹解析逻辑，捕获异常后记录日志并跳过当前条目。请求频率控制方面：DOWNLOAD_DELAY = 1.0 秒强制最小间隔，AUTOTHROTTLE 扩展可根据远端响应延迟动态调整间隔。

2.1.3 文本清洗与去重策略

从不同新闻平台采集到的原始 HTML 页面在结构、编码与标记风格上差异明显。以农业农村信息网为例，其正文嵌套于 <div class="TRS_Editor"> 节点中；而人民日报的搜索结果则可能分布在 <div id="Content"> 或 <article> 标签内。另一方面。页面里的导航、广告、脚本和版权声明这些噪音数据，必须通过清洗步骤予以剔除，否则将影响后续情感分析与词频统计的准确性。

本文的文本清洗由 DataCleaner 类统一实现，流程如图 2-3 所示。

图 2-3 HTML 文本清洗流程图
【待插入：raw HTML → lxml DOM解析 → script/style去除 → 多策略XPath降级提取(标题/日期/来源/正文) → clean_text规范化 → 结构化JSON 的流程图】

清洗阶段先用 `lxml.html.fromstring()` 将原始 HTML 转成 DOM 树，再通过 `xpath('//script|//style')` 删除脚本和样式节点，尽量先把“无用部分”清出去。之后再分别处理标题、日期、来源和正文四个字段。这里没有假设所有站点都遵循同一模板，而是采用“多策略依次尝试”的方式：标题优先读 `ArticleTitle`，读不到再退到 `og:title` 和 `title`；日期先取页面元数据，缺失时再结合 URL 规则推断；正文则在几个常见容器中顺序匹配。这样写虽然比单一路径提取稍显冗长，但对站点小幅改版的容忍度更高。字段抽出后，再交给 `clean_text()` 统一规整空格和空白字符。

提取完成后，clean_text() 方法对文本进行规范化处理：将全角空格 \u3000 和非断行空格 \xa0 统一替换为半角空格，利用正则表达式 \s+ 将连续空白字符合并为单个空格，并执行首尾空白去除。最终每条记录输出为包含序号、URL、标题、日期、来源和正文的结构化 JSON，供后续分析阶段消费。

去重策略在采集与持久化两个阶段执行，如图 2-4 所示。

图 2-4 两阶段去重策略示意图
【待插入：采集阶段 _seen_urls集合 O(1)内存去重 + 持久化阶段 merge_dedupe 跨文件合并去重的对比图】

去重并不是等到入库前才做，而是贯穿在前后两个阶段。第一阶段发生在采集时，`_seen_urls` 集合对详情链接先做快速过滤；第二阶段发生在持久化前，由 `merge_dedupe_llm_files()` 对不同文件中的结果再做一次整合。实际处理中，URL 优先作为主键，缺失时再退到标题。若判定为同一条新闻，则优先保留正文更完整的版本，同时把情感分和关键词做合并处理。考虑到输入文件体积可能较大，这一过程配合 `ijson` 进行流式读取，以免一次性载入全部数据造成内存压力。

2.1.4 分词统计与情感分析技术——Jieba 与 Qwen

在文本数据完成清洗后，需要从非结构化的新闻正文中提取可量化的统计指标。以支持舆情监测与趋势研判。本文主要从两个维度对文本进行深度加工：其一为基于 Jieba 的中文分词与词频统计，量化政策术语的分布；其二为基于Qwen 大语言模型的情感极性分析与关键词提取，用预训练语义能力对新闻做情感标注和关键词提取。

Jieba 是主流中文分词工具[4]，支持精确模式、全模式和搜索引擎模式三种分词算法。本文选用精确模式（jieba.cut(text, cut_all = False)）对清洗后的正文内容进行切分，该模式基于前缀词典与动态规划算法求解最大概率路径。分词与过滤流程如图 2-5 所示。

图 2-5 Jieba 分词与停用词过滤流程图
【待插入：jieba.cut → 正则[一-鿿]+ 校验 → 停用词表过滤(89词) → min_len≥2 → Counter 计数的流程】

完成分词后，系统不会直接统计所有 token，而是先做两轮过滤。第一轮是格式过滤，只保留中文字符序列和字母数字组合；第二轮是停用词过滤，把“的”“了”“记者”“责任编辑”这类高频但低信息量的词剔除出去。除此之外，还额外设置了 `min_len = 2`，尽量减少单字碎片对排名的干扰。过滤后的词项再统一交给 `collections.Counter` 做全局计数，最终得到 Top 300 高频词，并写入 `word_frequency_top.json`。这些统计结果后续既可以入库，也可以直接用于词云等可视化组件。

在情感分析部分，本文没有继续沿用纯词典匹配方案，而是改用阿里云 Qwen 模型作为语义分析引擎。这样做的主要原因，是政策新闻中很多表述并不适合简单地按词面褒贬来判断。例如，“加强监管”“稳步推进”在不同上下文里的语义强度并不一致，而大模型在上下文理解上更有优势。具体调用时，提示词把模型限定为“农村土地政策研究专家”，并要求其只返回 JSON 结果，例如 `{"score": 0.85, "keywords": ["承包权", "经营权", "流转费"]}`。其中 `score` 用于表达情感倾向，`keywords` 用于保留新闻中的核心概念。为兼顾成本和响应时间，输入正文只截取前 600 个字符，同时关闭深度思考模式。

在调用架构上，LlmAnalyzer 类实现了三层容错降级，如图 2-6 所示。

图 2-6 Qwen API 调用架构图
【待插入：SDK→REST→正则三层降级 + 同步BatchRunner/异步AsyncBatchRunner(100并发+ijson流式)双路径图】

调用策略采用了较为保守的三层降级设计。第一层优先走 DashScope 官方 SDK；如果 SDK 在网络层出现异常，再退到兼容模式的 REST API；如果 API 虽然返回了文本，但文本中夹杂了解释性内容，导致不能直接解析为 JSON，则再用正则表达式提取其中的 JSON 片段作为兜底。除此之外，系统同时保留同步和异步两种执行模式：同步模式适合小规模调试，便于观察单条结果；异步模式则更适合全量处理，通过 `Semaphore(100)`、`asyncio.gather()` 和流式读写机制明显压缩批量分析时间，同时保持结果输出的完整性。

2.1.5 关系型数据库 MySQL

经过采集、清洗与分析三个阶段处理后，每篇新闻均已具备结构化字段。本文选择 MySQL 作为后端存储引擎。其 UTF8MB4 字符集全面支持中文字符存储[22]，PyMySQL 与 executemany() 批量插入接口能以单次网络往返完成上千条记录的写入。数据库设计了两个核心表（字段详情与 E-R 图见 3.4 节）。

数据入库由 MySQLClient 类封装。提供 execute()、query() 与 insert_many() 三个核心接口。insert_many() 接收表名、列名与行字典迭代器，内部拼接 INSERT INTO ... VALUES 参数化 SQL 并通过 executemany() 批量提交，以默认 1000 条的批次大小控制单次事务数据量。建表操作通过 --create-table 参数触发，调用 CREATE TABLE IF NOT EXISTS 确保幂等性。
2.2 系统智能分析与应用技术基础

2.2.1 通义千问 Qwen 大模型接口

Qwen 是阿里云推出的大语言模型系列，覆盖了从轻量级到高性能的多个版本，可以适配不同的响应速度和成本要求。本文通过 DashScope 平台接入 Qwen 模型能力[21][21]。DashScope 对外提供统一的 REST API 与多语言 SDK，调用方式相对规范，适合直接嵌入现有 Python 项目，而不必自行部署推理服务。结合官方 SDK 与兼容 REST 两种模式，系统在实现上也保留了更大的灵活度。

本文以 qwen-flash 作为默认模型，响应延迟低。适合批量处理大量新闻。设置 enable_thinking=False 更深入地压缩输出 Token。

2.2.2 提示词工程与输出约束策略

提示词工程的核心，不是简单“告诉模型做什么”，而是把任务边界、输出约束和示例格式一次说清楚。在本文的情感分析场景里，提示词主要承担三项作用：先把模型角色限定在土地政策分析场景中，再把输出形式收紧为纯 JSON，最后给出一个明确的结果示例。这样做的目的很直接，就是降低模型自由发挥的空间，让返回结果更稳定、更容易被程序继续处理。

提示词的三段式组织结构与输出约束关系如图 2-8 所示。

图 2-8 提示词结构与输出约束示意图
【待插入：System Message 角色设定 + User Message 任务说明 + JSON 示例模板 三段式提示词结构图，并标注纯 JSON 输出约束与正则兜底解析关系】

具体来看，每次调用都由 `System Message` 和 `User Message` 共同组成。前者负责划定边界，例如明确要求“只返回 JSON”；后者则放入待分析的标题、正文片段、任务说明和格式示例。相比堆砌大量 few-shot 示例，这种“角色设定 + 任务说明 + 格式模板”的写法更短，也更适合批量处理。实际使用时，只要格式约束写得足够清楚，模型输出的稳定性就已经能够满足本系统需要。

在输出解析环节，系统没有假定模型每次都会“完美遵守格式”。正常情况下，返回值直接用 `json.loads()` 解析；如果模型偶尔带出解释文字，就再用正则提取第一个完整 JSON 对象。两次都失败时，不继续抛异常，而是记录日志并回退到中性分值与空关键词。这样处理的好处是，即使个别样本解析失败，也不会把整批任务卡住，整体流程会更稳。

2.2.3 桌面端框架 Electron 与 Vue

Electron 是开源桌面应用开发框架[7][22]。将 Chromium 内核与 Node.js 整合，使用 Web 技术栈构建跨平台桌面应用。Electron 双进程架构如图 2-10 所示。

图 2-10 Electron 双进程架构图
【待插入：Main Process + Renderer Process + Preload Script + IPC Bridge 的进程架构图】

应用由主进程与渲染进程组成：主进程负责窗口管理与系统 API。渲染进程承载 Web 页面。本文选用 Vue 3 作为渲染进程视图层，其 Composition API 与单文件组件（.vue）简化了复杂界面的组件化开发。应用端通过 Vite 实现热更新开发，结合 electron-builder 打包为各平台安装包。

2.2.4 Node.js 主进程与 IPC 通信

Electron 中主进程与渲染进程隔离运行，前者具备系统权限。后者受限在浏览器沙盒中，两者通过进程间通信（IPC）交换数据，如图 2-11 所示。

图 2-11 IPC 进程间通信示意图
【待插入：ipcRenderer.invoke → ipcMain.handle 双向通信 + contextBridge 安全暴露的机制图】

通信基于 ipcMain 与 ipcRenderer 两条通道实现。渲染进程通过 ipcRenderer.invoke() 发起请求，主进程通过 ipcMain.handle() 处理后返回结果。安全方面通过 contextBridge 与 preload 脚本建立受限 API 通道，将敏感操作封装于主进程侧，渲染进程仅调用预定接口。

2.2.5 数据可视化技术 ECharts

ECharts 是开源数据可视化库[8]，。在中文互联网领域使用广泛。ECharts 支持折线图、柱状图、饼图、散点图、地图等数十种图表类型，并提供缩放、平移、悬停提示、图例筛选等交互特性。

本文应用端中，ECharts 用于舆情趋势（折线图/柱状图按月统计新闻量）、情感分布（饼图正/中/负占比 + 堆叠面积图）、关键词词云（echarts-wordcloud 字号/颜色映射频次与情感）。配置驱动的 option 设计与 Vue 3 响应式系统结合，实现数据驱动的可视化更新。


2.2.6 文本到 SQL 转换技术

Text-to-SQL是指将用户输入的自然语言查询语句自动转换为结构化查询语言（SQL）的技术。基于 LLM 的 Text-to-SQL 方法通过在提示词中注入数据库 Schema 信息[6][23]，即可在零样本条件下生成语法正确的 SQL 语句[6][23]。

在本文的问答模块中，Text-to-SQL 的作用并不是单纯“把一句话翻成 SQL”，而是把用户模糊的中文问题转换为可执行、可解释的数据查询过程。系统大致分为三个步骤：先把数据库表结构注入提示词，让模型知道能查什么；再根据问题生成 SQL；最后结合查询结果生成面向用户的摘要解释。这样一来，用户不需要理解表结构，也不必手写查询语句，就能直接从新闻数据里得到较为明确的结论。

考虑到该模块允许模型参与生成 SQL，安全性必须放在优先位置。为此，系统至少设置了三道防线：第一道是语句级拦截，非 `SELECT` 查询一律不执行；第二道是执行层约束，通过结果上限和超时控制避免低效查询拖慢数据库；第三道是失败回退，当模型没有给出有效 SQL 时，不勉强执行，而是提示用户换一种问法。这样的处理虽然会牺牲少量“自由度”，但更符合实际部署时对安全和可控性的要求。
2.3 开发工具与工程化技术

本系统开发过程中使用了以下工具：Trae 与 Codex 作为智能编程辅助工具。Git 与 GitHub 进行版本管理与代码托管，Vite 作为前端构建工具提供快速 HMR 开发体验，Electron Builder 负责桌面应用的打包与安装程序生成。

第 3 章 系统需求分析与总体设计

3.1 系统可行性分析

系统采用的技术栈以成熟、易维护和便于协同为主要考虑，整体选型如图 3-3 所示。

图 3-3 系统技术选型总览图
【待插入：Data采集端(Scrapy+3Spider) → 清洗端(lxml+XPath) → 分析端(Qwen+Jieba) → 存储端(MySQL+PyMySQL) → 应用端(Electron+Vue3+ECharts) 五层技术栈全景图】

从组成上看，数据采集端采用 Scrapy，文本处理侧结合 Jieba 与 Qwen，存储部分使用 MySQL，应用端则基于 Electron + Vue 3 实现。之所以这样搭配，一方面是因为这些技术本身足够成熟，资料丰富，遇到问题容易排查；另一方面，它们之间的集成成本也相对可控。从实施条件看，系统开发依赖的多数软件和框架均可直接使用开源版本，持续性开销主要集中在 Qwen API 的调用费用上，整体仍处于本科毕业设计可以承受的范围内。

3.2 系统功能需求分析

根据本研究的目标定位与业务场景，系统功能需求划分为数据生产端与数据应用端两个子系统分别进行梳理。两者通过 MySQL 数据库是数据交换中枢，形成"后端生产、前端消费"的协作模式。

3.2.1 数据生产端需求

数据生产端（land-news-engine）承载从互联网数据获取到结构化数据入库的完整处理链路，其核心功能需求包括以下五个方面。

- 多来源并发采集。支持对农业农村信息网（agri.cn）、农业农村部（moa.gov.cn）与人民日报（chinadaily.com.cn）三个核心新闻平台的自动化数据采集。针对各平台差异化的请求方式（JSON 搜索 API、POST 表单搜索、RESTful 搜索接口）与响应格式。需要分别配置请求参数（包括关键词列表、页码范围、每页条数、排序规则）、会话 Cookie 与浏览器标识（User-Agent），确保能持续获取最新报道。采集过程需支持并发执行以减少总体耗时，并提供 URL 级别的内存去重以避免重复抓取。

- HTML 清洗与结构化提取。自动移除 script/style 等无关标签，多策略降级提取标题、日期、来源与正文四个字段。

- 智能语义分析。调用 Qwen 完成情感评分（0~1）与关键词提取（每篇 3 个）。支持同步/异步模式，具备 SDK→REST→正则三层容错降级。基于 Jieba 进行全量词频统计与停用词过滤。

- 去重合并。以 URL 或标题为键跨文件合并，保留最长正文、取最大情感分、并集关键词。

- 数据持久化。结构化数据通过 executemany 参数化批量导入 MySQL 双表，CREATE TABLE IF NOT EXISTS 确保建表幂等。

3.2.2 数据应用端需求

数据应用端（terminal-helper / DumbOx）是一套基于 Electron + Vue 3 的跨平台桌面应用程序，为终端用户提供可视化数据分析与交互操作界面。其核心功能需求涵盖以下六个方面。

- 舆情看板。包含四类视觉组件：情感评分分布环形饼图（按 >0.6 正面、0.4~0.6 中性、<0.4 负面分段）、情感趋势双 Y 轴折线图（双 Y 轴：月均情感评分 + 月新闻量）、媒体来源堆叠柱状图（Top 6 来源比例）、以及高频关键词情绪色彩词云（字号按频次缩放、颜色按情感着色）。

- 资料检索。分页浏览界面，支持按标题、来源、摘要与关键词的条件检索与排序。

- 智能问答。集成 Qwen 实现 Text-to-SQL：用户中文提问→模型生成 SQL→安全校验→执行查询→生成结论/证据/建议/风险/置信度五维结构化回答。拦截非 SELECT 语句，施加日期守护与结果上限。

- 终端命令管理。提供可创建、编辑、拖拽排序的命令卡片与集合，卡片按序执行并展示实时输出。

- 任务编排。多集合串联为任务，按序执行，显示进度条与完成日志，保留运行历史。

- 配置管理。图形界面配置 Shell 路径与 Qwen API Key/URL/Model，持久化存储于本地 JSON。

3.3 系统总体架构设计

本系统采用分层架构设计，自上而下分为数据源层、采集传输层、数据处理层、数据存储层与应用展示层五个层级，形成数据处理闭环。

数据源层由三个目标新闻网站构成：农业农村信息网（agri.cn）、农业农村部（moa.gov.cn）与中国日报/人民日报（chinadaily.com.cn）。

采集传输层基于 Scrapy 框架实现。本层部署了三个专用 Spider。分别针对三个数据源的搜索 API 格式与页面结构进行适配。Scrapy Engine 通过 Scheduler 调度请求、Downloader 异步下载响应，并将原始 HTML 页面以 JSON 格式输出至 data/raw 目录，完成从网络数据到本地文件的传输。

数据处理层承担的是“把原始网页变成可分析数据”的工作，也是整套系统里最核心的一层。这里由三个模块顺序衔接：`DataCleaner` 负责从 HTML 中抽取标题、日期、来源和正文；`LlmAnalyzer` 负责补充情感分值与关键词；`extractor` 负责做全量词频统计。前两步更偏向单篇新闻的结构化和语义化，最后一步则面向整体数据集提取统计特征。处理结果分别写入 `data/clean` 和 `data/stats` 目录，便于后续追踪和复用。

数据存储层以 MySQL 关系型数据库为核心。Persist 模块负责最终的数据去重合并与批量入库。将 LLM 分析结果映射为 land_news_analysis 表记录，将词频排名映射为 word_frequency_stats 表记录，完成从中间文件到持久化存储的转换。

应用展示层即 terminal-helper（DumbOx）桌面应用。基于 Electron + Vue 3 技术栈构建。Electron 主进程通过 IPC 通信机制桥接 MySQL 数据库查询、Qwen LLM 接口调用与本地 Shell 进程管理；Vue 3 渲染进程负责ECharts 图表）、资料检索（LibraryView）、智能问答（QaView）、终端管理（TerminalView）、命令卡片与任务调度（CardsView / CollectionsView / TasksView）以及SettingsView）等交互界面的渲染与用户事件响应。数据生产端（land-news-engine）与数据应用端（terminal-helper）通过共享同一 MySQL 数据库实例实现解耦协作：生产端负责数据的持续更新入库，应用端负责数据的智能查询与可视化展示。系统总体架构如图 3-2 所示。

图 3-2 系统总体架构图
【待插入：展示 Web 数据源 → Scrapy 采集层 → 清洗层 → LLM 分析层 → MySQL 存储层 → Electron 应用层 的五层分层架构图，标注各层的关键组件与数据流向】

3.4 数据库概念结构设计（E-R 图）

本系统的数据库设计以"新闻"为核心实体，以"词频"为辅助统计实体，形成一对多的关联关系。下面从实体属性与实体间关系的角度描述数据库概念结构。

新闻实体（land_news_analysis）是系统的核心数据实体。代表一条经过采集、清洗与智能分析后入库的完整新闻记录。其属性包括：自增主键 id（INT），是记录的唯一标识；文章链接 url（VARCHAR 500），存储新闻的原始 URL 地址。同时这同时也是跨来源去重的主要依据；标题 title（VARCHAR 500），存储清洗后提取的新闻标题文本；发布日期 publish_date（DATE），以 yyyy-MM-dd 格式记录新闻的发布时间。支持时间维度上的聚合查询与趋势分析；来源 source（VARCHAR 100），标识新闻所属的媒体平台名称（如"农业农村信息网""中国日报网"），用于来源对比统计；内容摘要 content_summary（TEXT），保存正文前 800 字符的摘要内容。供检索与概览使用；情感评分 sentiment_score（DECIMAL 4,3），以 0.000 至 1.000 的十进制精度记录 Qwen 模型输出的情感极性值，其中 >0.6 为正面、0.4~0.6 为中性、<0.4 为负面；关键词 keywords（JSON），以 MySQL 原生 JSON 类型直接存储关键词数组（如 ["承包权", "经营权", "流转费"]），无需额外创建关联表；创建时间 created_at（TIMESTAMP），自动记录数据入库的时间戳。

词频统计实体（word_frequency_stats）独立于新闻实体。存储基于全量正文统计得出的全局词汇出现频次。其属性包括：自增主键 id（INT）；词汇 word（VARCHAR 200），存储经 Jieba 分词并过滤停用词后的有效中文词汇；出现次数 count（INT），记录该词汇在全量新闻正文中的累计出现次数；创建时间 created_at（TIMESTAMP）。

两个实体通过 keywords 字段形成隐式多对多关联。这一关联在智能问答模块的 JSON_SEARCH / JSON_CONTAINS 查询中得以体现。从 E-R 图的视角来看，两个实体各自独立。通过关键词维度的查询实现跨表关联检索，结构简洁且查询语义清晰。系统数据库 E-R 图如图 3-1 所示。

图 3-1 系统数据库 E-R 图
【待插入：包含 land_news_analysis 与 word_frequency_stats 两个实体的 E-R 图，标注各字段属性及实体间的关键词多对多关联】

3.5 核心业务逻辑设计

本系统的核心业务逻辑，可以概括为“一条新闻如何从网页原文走到用户可见的图表与问答结果”。这一过程横跨数据生产端和数据应用端，两部分既相互独立，又通过数据库保持衔接。下面按数据流顺序说明其主要环节。

首先是采集。Spider 按关键词和页码组合构造搜索请求，列表页回调负责发现详情链接，详情页回调负责抓取 HTML 正文，最后通过 Feed Exports 统一输出到 `data/raw`。这一阶段的目标并不是马上做复杂分析，而是先把原始数据尽量稳定地落到本地。

随后进入清洗与分析阶段。`DataCleaner` 从 `data/raw` 中读取原始记录，解析 HTML、剔除噪声节点并提取关键字段，再将结果写入 `data/clean`。在此基础上，`LlmAnalyzer` 为每条新闻补充情感评分与关键词，输出 `_llm.json` 文件；`extractor` 则独立处理 `_clean.json`，对正文做分词和词频统计，最终生成 `word_frequency_top.json`。这几步虽然相互衔接，但文件输出彼此独立，便于在某一环节出问题时单独重跑。

再下一步是持久化入库。Persist 模块扫描 data/clean 目录下的所有 _llm.json 文件。对每条记录执行字段标准化与去重合并后，经 to_mysql_rows 映射为表结构行字典，通过 executemany 批量入库；词频数据则通过 --import-wordfreq 参数独立导入 word_frequency_stats 表。该部分的工程细节将在第 4.3 节结合图 4-6 展开。这里先提一点：五段数据流中，第三段（去重合并）踩的坑最多，不同来源的新闻标题有时被截断或加了后缀。纯字符串匹配的去重经常漏掉同一篇文章，后来在 merge_records 里加了最长正文优先和情感分取最大值的逻辑才基本稳定。

最后是应用层消费数据。`terminal-helper` 启动后，先从本地读取配置与卡片数据；当用户打开看板时，渲染进程通过 IPC 请求主进程查询数据库，再将结果交给 ECharts 渲染；当用户发起自然语言问答时，系统再进入 Text-to-SQL 流程：先生成 SQL，再做安全校验，然后执行查询，最后把查询结果整理成结构化回答返回界面。也就是说，应用端不是直接操作底层数据文件，而是统一通过主进程和数据库完成数据访问，这样职责划分更清晰。



第 4 章 数据生产端（land-news-engine）的设计与实现

4.1 采集模块实现

采集模块是数据生产端的起点，任务是把分散在不同新闻网站上的土地流转报道稳定抓取下来。整个模块建立在 Scrapy 框架之上，由三个站点专用 Spider、统一配置文件以及 Feed Exports 输出机制共同组成。

4.1.1 针对不同新闻平台的爬虫策略（多端适配）

本文选取的三个新闻来源在搜索接口、返回格式和页面结构上差异都比较明显，因此不能用一套规则直接覆盖全部站点。实际实现时，每个 Spider 都做了针对性的适配，但仍共享同一套关键词列表和基础配置，以减少重复代码。

农业农村信息网爬虫 `AgriInfoScrapySpider` 面向 agri.cn 的 JSON 搜索接口实现。该站点采用 GET 请求，搜索条件主要通过 URL 参数传递，如频道编号、关键词、页码和排序方式等。程序在 `start_requests()` 中按“页码 × 关键词”的组合生成请求，再交由 Scrapy 引擎统一调度。因为该站点对未登录访问较为敏感，请求中还需要同时携带预设 Cookie 与浏览器头信息。列表回调 `parse_list()` 从返回的 `items` 数组中提取详情页链接，并先经过 `_seen_urls` 去重；详情回调 `parse_detail()` 则负责抓取原始 HTML，并与 URL、来源标识一起封装为输出记录。

农业农村部爬虫 `MoaScrapySpider` 针对的是 POST 型搜索接口。该站点的检索服务依托统一检索系统实现，因此请求体中需要携带站点编号、关键词和页码等表单参数。这里使用 `scrapy.FormRequest` 模拟浏览器提交行为，同时补充会话标识和防 CSRF 相关信息。相较之下，该站点详情页的正文区域更稳定，页面提取压力小一些，但整体仍与后续通用清洗流程保持兼容。

人民日报/中国日报爬虫 `RMRBScrapySpider` 对接的是 chinadaily.com.cn 的 REST 风格搜索接口。该接口返回的是结构化 JSON，其中每条记录的 `url` 字段即可直接作为详情页地址。与 agri.cn 类似，这个站点也依赖 Cookie 维持会话，且不同栏目、不同时间段的详情页结构并不完全一致。因此程序将关键词、请求头和 Cookie 参数统一放在配置文件中管理，尽量把“站点差异”控制在配置层，而不是散落在代码里。

三个 Spider 在运行层面共用一套核心参数。例如，`DOWNLOAD_DELAY` 用于降低请求过密带来的封禁风险，`CONCURRENT_REQUESTS` 用于控制并发规模，`RETRY_TIMES` 和 `DOWNLOAD_TIMEOUT` 则主要服务于异常恢复。爬虫抓到的结果最终由 Feed Exports 统一导出到 `data/raw` 目录，作为后续清洗模块的原始输入。Spider 的运行日志示例如图 4-7 所示。

图 4-7 Scrapy Spider 运行输出截图
【待插入：终端中 scrapy runspider 运行日志截图，展示 start_requests 日志行 + Item 抓取统计 + Feed Exports JSON 输出确认】

该截图为后续清洗环节提供统一的文件格式入口。以下为 Scrapy Spider 的核心配置与回调链结构示例：

```python
class AgriInfoScrapySpider(scrapy.Spider):
    name = "agri_info_scrapy"
    custom_settings = {
        "DOWNLOAD_DELAY": 1.0,
        "CONCURRENT_REQUESTS": 16,
        "RETRY_TIMES": 2,
        "DOWNLOAD_TIMEOUT": 10,
    }

    def start_requests(self):
        for page in range(self.pages):
            for keyword in self.keywords:
                yield scrapy.Request(
                    url=f"{self.base_url}?keyword={keyword}&page={page}",
                    cookies=self.cookies,
                    headers=self.headers,
                    callback=self.parse_list,
                )

    def parse_list(self, response):
        items = response.json().get("items", [])
        for item in items:
            url = item.get("docpuburl")
            if url and url not in self._seen_urls:
                self._seen_urls.add(url)
                yield scrapy.Request(
                    url=url,
                    callback=self.parse_detail,
                )

    def parse_detail(self, response):
        yield {
            "URL": response.url,
            "内容": response.text,
            "来源": "农业农村信息网",
        }
```

4.1.2 异常处理机制（反爬应对、重试策略）

网络爬虫在真实环境里几乎不可能一直顺利运行。目标网站波动、访问频率受限、页面结构调整以及临时网络故障，都可能让抓取任务中断。因此，采集模块必须具备一定的自动恢复能力，而不是依赖人工盯着日志逐一处理。

Scrapy 异常处理机制的工程落地层次如图 4-2 所示。

图 4-2 Scrapy 异常处理机制层次图
【待插入：RetryMiddleware 自动重试 + DOWNLOAD_TIMEOUT 超时控制 + Spider try/except + errback 兜底 + AUTOTHROTTLE 调速 五层容错图】

在下载器这一层，主要依赖 Scrapy 自带的 `RetryMiddleware` 和超时控制来兜底。程序设置了 `RETRY_TIMES=2`，遇到 HTTP 5xx、连接超时等异常时，会自动再次尝试请求，而不需要在每个回调里手动补重试逻辑。`DOWNLOAD_TIMEOUT=10` 则限制了单次请求等待时长，超时后同样会进入重试流程。若重试后仍然失败，就交由 `errback` 记录错误链接和异常信息，留待后续人工排查。

在请求频率控制方面，`DOWNLOAD_DELAY=1.0` 先给每个域名设定一个比较保守的最小请求间隔，把抓取节奏压住。若继续启用 `AUTOTHROTTLE`，系统还可以根据目标站点的响应时间动态调整访问速度。简单来说，站点响应越慢，请求节奏就越保守；响应恢复正常后，再适当加快。这样做更符合真实运行时“先稳住，再提速”的思路。

到了 Spider 回调层，`parse_list()` 和 `parse_detail()` 又做了一层更细的异常捕获。比如，搜索接口返回的 JSON 结构不完整，或者详情页改版后导致 XPath 规则失效，程序都不会直接中止，而是把错误写入日志后跳过当前条目。这样做的重点不是“保证每一条都成功”，而是保证少量异常不会把整批任务拖死。

4.2 文本预处理与智能分析实现

文本预处理与智能分析承担的是“把原始网页内容转成可用语义数据”的任务。整个环节由文本清洗、LLM 分析和词频统计三个子模块依次完成，前后衔接较紧。

4.2.1 文本清洗流程（去噪、去重逻辑）

文本清洗模块由 `processor/cleaner.py` 中的 `DataCleaner` 类实现，负责把爬虫抓到的原始 HTML 变成结构化字段。整体流程可以概括为三步：先解析 DOM 并去掉噪声节点，再提取标题、日期、来源和正文，最后统一整理文本格式。

第一步是 DOM 解析与去噪。`DataCleaner` 使用 `lxml.html.fromstring()` 把 HTML 字符串解析成可遍历的 DOM 树，再通过 `tree.xpath('//script|//style')` 找出脚本和样式节点并删除。这样做可以先把最容易污染正文抽取结果的部分清理掉，避免后面在字段提取时混入大量无关内容。

第二步是多策略字段提取。这里没有把标题、日期、来源和正文绑定到单一 XPath，而是为每个字段准备了多个候选路径，按优先级依次尝试，命中后就立即返回。比如标题会优先检查特定 `meta` 标签，找不到再退到页面标题；日期先读元数据，实在缺失时再从 URL 里推断。正文则在几个常见容器中顺序匹配。相比“只写一条 XPath”的做法，这种降级方式在面对站点改版时更耐用。

第三步是文本规范化。提取出的字段统一交给 `clean_text()` 处理，将全角空格和非断行空格替换为普通空格，再把连续空白压缩为单个空格，最后去掉首尾无效字符。经过这一轮整理后，每条记录会以结构化 JSON 的形式写入 `data/clean` 目录，供后续分析模块继续使用。

4.2.2 基于 Qwen API 的情感极性标注与关键词提取

情感分析与关键词提取由 `processor/analyzer.py` 中的 `LlmAnalyzer` 类完成。该模块通过调用阿里云 DashScope 的 Qwen 模型，为清洗后的新闻补充情感评分和领域关键词。

在调用架构上，`LlmAnalyzer` 沿用了前文提到的三层降级策略。第一层优先走 DashScope 官方 SDK；如果 SDK 在网络层或底层连接上出问题，再退到兼容模式 REST API；如果接口虽然返回了文本，但文本里夹杂解释性语句，无法直接用 `json.loads()` 解析，就继续使用正则表达式提取其中的 JSON 片段。若这三层都失败，系统会把当前记录回退到默认情感分和空关键词，而不是让整个批处理任务停在中途。

在提示词设计上，系统没有给模型过多自由发挥空间，而是把输出边界写得很明确。`System Message` 直接规定“只返回 JSON”，`User Message` 再补充新闻标题、正文片段、任务说明和结果示例。这样做的重点是稳定格式，而不是追求华丽回答。与此同时，系统关闭了 `enable_thinking`，进一步压缩输出 token。在批量调用场景下，这一设置对成本控制和响应速度都有比较直接的帮助。

在批量处理方面，系统保留了同步和异步两种模式。同步模式更适合调试，便于逐条观察结果；异步模式则用于正式跑全量数据，通过 `Semaphore(100)` 限制并发规模，并以 `batch_size=50` 为单位批量提交任务。再配合 `ijson` 流式读取和 `JsonArrayWriter` 流式写入，系统既避免了大文件一次性载入带来的内存压力，也把网络等待时间充分叠加起来。实际测试中，异步模式对总耗时的改善非常明显。

4.2.3 词频统计与领域词库构建

词频统计由 `processor/extractor.py` 独立完成，其任务是对全量清洗后的新闻正文进行分词和频次汇总。

词频统计模块的工程处理链如图 4-5 所示。

图 4-5 Jieba 词频统计流程图
【待插入：clean.json 正文 → jieba.cut → 正则校验 + 停用词过滤 → Counter 计数 → most_common(300) → word_frequency_top.json 全流程】

在分词阶段，系统使用 Jieba 精确模式处理每篇新闻正文。分词结果不会直接参与统计，而是先经过格式过滤和停用词过滤。前者主要清除符号、空白和异常 token，后者则专门剔除高频但低信息量的虚词、报道性动词和杂项词。除此之外，还设置了 `min_len=2`，尽量避免单字碎片占据词频排名。经过这几轮筛选后，留下来的词项更接近真正具有分析价值的内容。

统计汇总阶段，所有有效词项统一交给 `collections.Counter` 做全局累加，最后通过 `most_common(300)` 取出前 300 个高频词，写入 `data/stats/word_frequency_top.json`。这部分结果后续可以继续导入 `word_frequency_stats` 表，也可以直接被看板词云和关键词检索功能使用。

4.3 数据持久化模块实现（MySQL 入库与索引优化）

数据持久化模块是数据生产端的最后一道工序，由 `storage/db_client.py` 与 `processor/persist.py` 协同完成，核心工作包括去重整合、字段标准化以及批量入库。

数据持久化的工程流程如图 4-6 所示。

图 4-6 数据持久化入库流程图
【待插入：_llm.json → normalize_llm_record → merge_dedupe → to_mysql_rows → executemany → MySQL 双表，全流程展示词频表独立导入分支】

`MySQLClient` 类封装了基于 PyMySQL 的数据库访问逻辑，提供了执行 DDL、查询结果和批量插入等常用接口。其中，`insert_many()` 是入库过程里最关键的方法：它会根据表名和列名动态拼接参数化 SQL，再调用 `executemany()` 一次性提交整批数据。默认批次大小设为 1000 行，既能保证写入效率，又不至于把单笔事务做得过大。连接层统一使用 UTF8MB4，以兼容中文和特殊字符存储。

在真正写库之前，`persist.py` 会先做两步预处理。第一步是去重合并。程序扫描 `data/clean` 目录下的 `_llm.json` 文件，优先以 URL、其次以标题作为去重键。如果发现同一新闻存在多个版本，就尽量保留信息更完整的那一条，并同步合并情感分与关键词。这一步的目的很明确，就是尽可能减少重复转载给后续统计带来的干扰。

第二步是字段标准化，也就是 `to_mysql_rows()` 的工作。这里会把日期统一整理成数据库可接受的格式，把 `keywords` 序列化成 JSON 字符串，把情感分转换成兼容 `DECIMAL(4,3)` 的数值，并截取正文前 800 个字符作为摘要字段。建表动作则由 `--create-table` 触发，通过 `CREATE TABLE IF NOT EXISTS` 保证重复执行时不会产生表结构冲突。

词频数据的导入则相对独立。程序在收到 `--import-wordfreq` 参数后，读取 `data/stats/word_frequency_top.json`，将其中的 `word` 和 `count` 解析为标准化行字典，再按批次切分后写入 `word_frequency_stats` 表。这样做可以把“新闻正文入库”和“词频数据入库”拆开处理，调试时也更灵活。

第 5 章 数据应用端（terminal-helper）的设计与实现

5.1 可视化看板实现

可视化看板是数据应用端最直观的展示入口。它由 `BoardView.vue` 与 `App.vue` 中的图表管理逻辑共同实现，负责把数据库中的结构化新闻数据转成用户可以直接理解的图表信息。看板所需数据统一由主进程的 `board:fetch-data` 处理器查询后返回给渲染进程。

5.1.1 舆情热度趋势与分布展示

舆情看板整体布局如图 5-1 所示，包含饼图、趋势线、柱状图与词云四个组件。

图 5-1 舆情可视化看板整体截图
【待插入：terminal-helper BoardView 看板页面完整运行截图（含环形饼图+双Y轴趋势线+来源柱状图+情绪色彩词云+左上KPI卡片）】

舆情热度趋势通过两个维度的图表加以展示：按月的新闻发布量变化曲线与媒体来源分布。

在月度趋势展示中，系统使用双 Y 轴折线图同时呈现“月均情感分”和“月度新闻量”。主进程先从 `land_news_analysis` 取出原始数据，再由渲染进程按月份聚合。左轴显示平均情感分，右轴显示新闻数量，横轴则按时间顺序排列。将这两个指标放在同一张图里，可以更直观地观察情感波动与新闻热度是否同步变化。

在媒体来源分布部分，看板采用堆叠柱状图展示发布量最高的前 6 个来源及其情感构成。每根柱子对应一个媒体平台，内部再按正面、中性和负面三类进行颜色区分。相比只看“谁发得多”，这种展示方式还能顺带观察不同媒体在相关议题上的情感倾向差异。

看板左上角的“舆情晴雨表”区域采用环形饼图展示整体情感分布，同时在图心位置叠加平均情感分值。下方再配合 KPI 卡片补充极端情绪条目数等指标。这样一来，用户一进入页面就能先抓住整体分布，再决定是否继续深入查看趋势和来源结构。

5.1.2 情感演变及关键词云图展示

关键词词云是看板中的另一项重要展示内容。它没有直接使用 ECharts 词云插件，而是采用 CSS 渲染方式实现，以获得更灵活的字号、颜色和交互控制。词云数据来自 `word_frequency_stats` 表中的高频词。

主进程会额外查询词频表，并按频次降序返回候选词。渲染进程再把 `count` 映射为字号，把关联新闻的平均情感分映射为颜色，同时借助透明度增强层次感。用户悬停在词项上时，还能看到对应频次和情感信息。整个词云因此不只是“词出现得多不多”，还带有一定的语境提示。

这一设计的特别之处在于，词云并非只按频次放大词项，而是进一步把情感信息编码进颜色中。换句话说，用户不仅能看到哪些词出现得最频繁，还能大致判断这些高频词更多出现在积极语境还是消极语境里。相比传统词云，这种呈现方式的信息密度更高。

除看板外，系统还提供新闻资料检索界面 `LibraryView`。用户可以按标题、来源、摘要和关键词等条件进行筛选，并按页浏览结果。每条新闻都保留了 URL、日期、情感分值、来源和关键词标签等信息，便于进一步查看和比对。

图 5-2 新闻资料检索界面截图
【待插入：LibraryView 分页浏览页面截图，展示搜索栏、新闻列表、关键词标签和详情展开】

从实现方式看，看板中的图表实例统一交由 `App.vue` 中的 `ChartManager` 管理。组件挂载时完成初始化和事件绑定，组件卸载时再统一释放资源。这样的做法把图表渲染逻辑从具体页面中抽离出来，后期维护会更清晰，也能避免重复初始化带来的资源浪费。

5.2 智能问答系统实现（重点章节）

智能问答模块是数据应用端最具辨识度的功能之一。它把大模型的语言理解能力和 MySQL 的结构化查询能力结合起来，形成“自然语言提问 - 生成 SQL - 查询数据库 - 返回摘要”的完整链路。具体实现上，前端由 `QaView.vue` 负责交互，主进程中的 `qa:ask` 处理器负责组织整个问答流程。

5.2.1 自然语言转化为 SQL 逻辑（Text-to-SQL）

Text-to-SQL 的第一步是让模型先把中文问题转成 SQL。用户在 `QaView.vue` 中输入问题后，渲染进程通过 `ipcRenderer.invoke('qa:ask', { question })` 把请求交给主进程。主进程再读取用户配置的模型参数，构造与 DashScope 兼容的调用连接，并把问题和 SQL 生成提示词一起送入模型。

`QWEN_SQL_SYSTEM_PROMPT` 是这一过程中的关键约束。提示词会明确告诉模型：可访问的只有 `word_frequency_stats` 和 `land_news_analysis` 两张表，输出必须是单条 `SELECT` 语句，不能附带解释文字，也不能生成任何修改性 SQL。对于那些看上去更像咨询类的问题，系统也不会让模型直接拒答，而是尽量把问题转换成可落到现有数据上的查询任务；只有当问题确实无法映射时，才返回预设的兜底回复。

第二步是安全校验，即 `validateSelectSql` 的工作。模型返回的 SQL 会先被提取出来，再逐层检查：是否包含注释注入、是否出现非 `SELECT` 危险语句、是否访问了白名单之外的表、是否需要补日期守护和结果上限。程序还会从用户原始问题里识别年份范围，以便在必要时补充对应的时间条件。只有通过这些检查后，SQL 才会真正送去数据库执行。

以下为 validateSelectSql 安全校验的核心逻辑伪代码：

```javascript
function validateSelectSql(rawSql, question) {
  // 1. 提取 SQL 代码块
  let sql = extractSqlFromCodeFence(rawSql);
  // 2. 拦截注释注入
  sql = sql.replace(/--.*$/gm, '').replace(/\/\*[\s\S]*?\*\//g, '');
  // 3. 拒绝非 SELECT 危险关键词
  const forbidden = /(UPDATE|DELETE|INSERT|DROP|ALTER|TRUNCATE|CREATE|REPLACE|GRANT|REVOKE)/i;
  if (forbidden.test(sql)) throw new Error('不允许执行非查询操作');
  // 4. 白名单校验表名
  const allowedTables = ['land_news_analysis', 'word_frequency_stats'];
  const usedTables = sql.match(/FROM\s+`?(\w+)`?/gi);
  if (usedTables && !usedTables.every(t => allowedTables.includes(t))) {
    throw new Error('仅允许查询指定表');
  }
  // 5. 注入日期守护 (默认近一年)
  if (!sql.toUpperCase().includes('WHERE') && !hasExplicitYear(question)) {
    sql += ` WHERE publish_date >= DATE_SUB(CURDATE(), INTERVAL 1 YEAR)`;
  }
  // 6. 追加结果上限
  if (!sql.toUpperCase().includes('LIMIT')) {
    sql += ' LIMIT 200';
  }
  return sql;
}
```

第三阶段为 SQL 执行与结果返回。校验通过的 SQL 通过 mysql2/promise 连接池在 MySQL 数据库上执行，主进程将查询结果集（行数组）与原始问题、执行过的 SQL 语句一并传入第二阶段的大模型调用。

5.2.2 基于查询结果的动态摘要生成

SQL 执行完成后，系统会启动第二次 Qwen 调用，用于把结果整理成结构化中文摘要。这里使用的是另一套提示词 `QWEN_SUMMARY_SYSTEM_PROMPT`，其输出模板固定为：

结论：基于数据给出清晰的判断性语句。
证据：从查询结果中提取支撑结论的具体数据证据。
建议动作：基于分析结果给出操作层面的行动建议。
风险等级：评估当前舆情或趋势的风险程度。
置信度：说明基于数据量的置信程度。

这里采用“二次调用”而不是一步到位，原因在于两项任务关注点并不相同。第一次调用更强调 SQL 的准确性，第二次调用更强调语言表达和结果解释。把这两件事拆开后，模型在每一步的目标都更单一，实际效果也更稳定。

智能问答模块的界面呈现效果如图 5-3 所示。

图 5-3 智能问答界面与可追溯 SQL 展示截图
【待插入：QaView 问答界面截图，展示问题输入区、六格决策卡片与可追溯 SQL 面板】

渲染进程收到 `qa:ask` 的结果后，会在 `QaView.vue` 中把内容组织成六格决策卡片，分别展示问题、结论、证据、建议动作、风险等级和置信度。卡片下方还会额外给出原始 SQL 和推断出的时间范围，让用户可以回看系统到底查了什么，而不是只看到一句结论。

5.3 通用业务模块统一设计与实现

除看板和智能问答外，数据应用端还提供了一套终端命令管理系统。它通过“卡片 - 集合 - 任务”三层抽象，把原本分散的命令执行过程组织起来，方便用户按步骤运行和回看结果。

5.3.1 爬虫命令与集合存储模块

命令卡片（Card）是本系统中最基础的执行单元，由 CardsView.vue 组件负责管理。卡片管理界面如图 5-4 所示。

图 5-4 命令卡片管理界面截图
【待插入：CardsView.vue 三列网格布局 + FAB浮动按钮组的卡片管理界面截图】

每张命令卡片至少包含四个核心属性：名称、命令字符串、执行目录和停止动作。界面上采用三列网格展示，并支持增删改查和拖拽排序。这样处理后，原本零散的终端命令就变成了可管理、可复用的操作单元。

命令集合 `Collection` 是多张卡片的组合容器，由 `CollectionsView.vue` 负责管理。一个集合对应一条有序的执行链，必要时还可以配置集合级停止动作。集合启动后，主进程会创建共享 Shell 会话，按顺序执行其中的每一张卡片，并把每一步的输出和状态实时推回前端界面。

卡片和集合的数据统一由 Pinia 管理。前端对状态的每次修改，都会触发持久化逻辑，通过 IPC 把最新快照发送给主进程，再由主进程写入本地 JSON 文件。这样做虽然简单，但对桌面应用来说足够稳定，也便于后续迁移和备份。

5.3.2 任务执行与运行记录模块

任务（Task）位于卡片-集合三层模型的最上层。一个任务由多个命令集合按预定顺序串联组成。任务执行界面如图 5-5 所示。

图 5-5 任务编排与执行界面截图
【待插入：TasksView.vue 任务列表 + 进度条 + 完成徽章动画 的执行界面截图】

由 TasksView.vue 组件管理。用户在界面中创建任务时，可选择已存在的多个集合并按拖拽排序决定其执行顺序。

任务执行由 `useTaskRunner.js` 驱动。用户触发任务后，系统会按集合顺序逐一运行，每完成一部分就更新进度条和状态徽章。所有集合跑完后，任务被标记为完成，同时把启动时间、结束时间和运行状态等信息写入历史记录。这样一来，任务不仅能执行，还能回溯。

日志的实时推送是这一模块能否好用的关键。主进程执行命令时，会持续监听 `stdout` 和 `stderr`，一旦收到输出块，就立刻通过 IPC 推给渲染进程。前端收到消息后同步刷新界面，因此用户能够边执行边观察结果。针对 Windows 下常见的中文乱码问题，系统额外引入 `iconv-lite` 处理编码识别。

5.3.3 终端会话与输出展示模块

终端模块属于系统的底层能力之一，由 `TerminalView.vue` 和 `useTerminalRuntime.js` 共同实现。终端界面如图 5-6 所示。

图 5-6 多标签终端会话界面截图
【待插入：TerminalView.vue 多标签页 xterm.js 终端窗口截图，展示 Shell 命令执行与输出】

具体实现上，前端使用 `xterm.js` 负责终端渲染，后端使用 `node-pty` 创建伪终端进程，两者通过 Electron IPC 进行桥接。这样既能保留本地 Shell 的真实执行能力，又能把终端输出稳定地展示到应用界面里。

终端创建时，渲染进程先通过 `terminal:create` 请求主进程生成 PTY 会话；主进程调用 `pty.spawn()` 启动子进程，并为其分配唯一的 `terminalId`。此后，PTY 的输出会持续通过 `onData` 回调被捕获，再经 IPC 推回前端，最终写入 `xterm.js` 的显示缓冲区。整个过程对用户来说基本是无感的，看起来就像应用内部嵌入了一个真实终端。

终端还支持多标签页管理。每个标签页都对应独立的 PTY 会话和 `xterm.js` 实例，用户可以自由创建、切换和关闭。关闭标签时，前端会通知主进程结束对应子进程，避免会话资源长期残留。

终端尺寸自适应依赖 `xterm-addon-fit` 完成。每次容器大小变化或标签切换后，程序都会重新计算终端行列数并同步给 PTY 后端，以保证显示布局尽量一致。另一方面，终端不仅是独立功能页，也承担卡片和任务执行时的输出窗口角色，用户可以在这里直接观察命令的全过程。

5.3.4 应用配置与模型参数管理模块

应用配置模块由 SettingsView.vue 组件承载，管理系统运行所需的两个核心参数组。配置界面如图 5-7 所示。

图 5-7 应用配置管理界面截图
【待插入：SettingsView.vue Shell路径检测 + Qwen API Key/URL/Model 配置表单界面截图】

配置界面主要承载两类信息：一类是本地 Shell 工具路径，另一类是 Qwen API 的连接参数。

在 Shell 路径配置部分，界面分别提供了 CMD、PowerShell 和 Bash 的路径输入项。用户既可以手动填写，也可以通过“检测”按钮让系统自动搜索本机可执行路径；填写后再通过“验证”按钮检查路径是否真实存在、是否为绝对路径。这样一来，用户不必自己在文件系统里反复查找安装位置。

Qwen API 配置部分则提供了 API Key、Base URL 和 Model 三项输入。所有配置统一挂在 Pinia 的 `settings` 对象上，并在修改后自动写入本地 JSON 文件，因此应用重启后仍能保留上一次的设置。

从结构上看，`SettingsView.vue` 只负责表单展示和用户输入采集，真正的持久化、检测和校验逻辑都放在主进程或 store 层处理。这样能让界面层保持相对简单，后续维护也更方便。

第 6 章 系统测试

6.1 测试目的

系统测试的目的，是从数据生产端和数据应用端两个方向验证系统是否可用、是否稳定，以及是否具备基本安全性。十组测试场景的覆盖范围如图 6-1 所示。

图 6-1 系统测试场景覆盖概览图
【待插入：十组测试 TC01-TC10 按采集→清洗→分析→入库→可视化→安全六个维度分组的覆盖范围图】

测试目标主要包括五个方面：一是确认采集、清洗、分析和入库链路能够完整跑通；二是检查字段提取、情感评分和关键词结果是否具有基本合理性；三是观察并发配置下系统的运行稳定性；四是验证 Text-to-SQL 模块对危险查询的拦截效果；五是核对应用端图表、检索和命令执行结果是否与底层数据保持一致。

6.2 场景化测试用例设计

为系统化地覆盖上述测试目标，以下设计了十组具有代表性的测试场景与验证标准，如表 6-1 所示。

表 6-1 系统测试用例汇总表
| 编号 | 测试场景 | 测试方法与输入 | 预期结果 | 实际结果 |
|--，，|，，，，-|，，，，，，，|，，，，-|，，，，-|
| TC01 | Scrapy 多源采集成功率 | 运行三个 Spider，各抓取 3 页搜索结果，统计详情页成功条数 ÷ 提取 URL 总数 | 成功率 ≥ 90% | 通过：10750/11108，成功率 = 96.78% |
| TC02 | HTML 清洗核心字段提取准确率 | 随机抽取 50 条清洗后记录，人工对照原始 URL 浏览器渲染结果，逐一核对标题、日期、来源与正文主体提取结果 | 核心字段准确率 ≥ 90%，且正文主体无明显缺失 | 通过：核心字段准确率达到预期，且正文主体无明显缺失 |
| TC03 | Qwen 原始响应 JSON 合规率 | 在 Analyzer 阶段统计模型原始返回结果中可直接解析为合法 JSON 的响应条数 ÷ 总调用条数 | 原始响应 JSON 合规率 ≥ 95% | 通过：原始响应 JSON 合规率达到预期 |
| TC04 | Jieba 词频 Top10 合理性 | 人工审查 Top10 高频词，确认是否以土地流转与农业政策相关实义词为主，且不含停用词与明显噪声词 | Top10 以领域相关实义词为主，且无停用词与明显噪声词 | 通过：Top10 全部为领域术语 |
| TC05 | MySQL 入库行数一致性 | 统计去重合并后记录数 N1，与 MySQL 表中记录数 N2 比对 | N1 = N2（误差 ≤1） | 通过：去重合并后记录数与 MySQL 表中记录数一致 |
| TC06 | 看板图表数据匹配 | 记录饼图总值/平均分/来源排名，与直接查库结果比对 | 图表数据与直查完全一致 | 通过：图表数据与直查完全一致 |
| TC07 | Text-to-SQL 语义正确性 | 设计 5 个覆盖来源统计、时间过滤、情感筛选、关键词搜索与词频排名的自然语言问题，检查生成 SQL 是否与问题意图一致 | ≥4 个 SQL 语义正确，且查询结果与问题意图一致 | 通过：5 个问题中多数 SQL 语义正确，且查询结果与问题意图一致 |
| TC08 | 非法 SQL 拦截测试 | 提交含 UPDATE、DELETE、DROP 等危险关键词的恶意"问题" | 拦截率 100%，数据库无异常记录 | 通过：危险 SQL 全部被成功拦截，数据库无异常记录 |
| TC09 | ijson 流式读取内存优化效果 | 选取超过 5000 条记录的 JSON 文件，分别采用 json.load() 与 ijson.items() 两种模式运行统计脚本并记录内存峰值 | 流式模式内存峰值明显低于全量模式 | 通过：流式模式内存峰值明显低于全量模式 |
| TC10 | 端到端全链路执行 | 从零依次执行 Spider → Cleaner → Analyzer → Extractor → Persist 五个脚本 | 全链路无 fatal error 中断，数据可被终端应用正常读取 | 通过：全链路无 fatal error 中断，数据可被终端应用正常读取 |

通过上述十组测试场景的设计与执行，系统在功能完整性、数据准确性、并发稳定性与安全性四个核心维度得到了较为全面的验证，测试结果达到了预定的合格标准。

第 7 章 总结与展望

7.1 全文工作总结

本文围绕农村土地流转新闻文本的自动化采集、智能分析与可视化交互这一目标，设计并实现了一套由数据生产端和数据应用端共同组成的协同系统。整个研究与开发过程主要完成了以下几方面工作。

在技术选型与理论基础方面，本文围绕 Scrapy、Jieba、Qwen、MySQL、Electron、ECharts 以及 Text-to-SQL 等关键技术进行了梳理，为后续的系统实现提供了方法依据和工程参照。

在数据生产端，本文搭建了较完整的“采集-清洗-分析-存储”处理链。具体来说，先用 Scrapy 对三类新闻站点进行自动化采集，再用 lxml 和 XPath 规则提取标题、日期、来源及正文，之后结合 Qwen 与 Jieba 完成情感和词频分析，最后通过 PyMySQL 批量写入数据库。

在数据应用端，本文基于 Electron 与 Vue 3 实现了一个可直接运行的桌面工具，集成了看板、检索、问答、终端和配置五类功能。其中，看板负责展示统计结果，问答负责把自然语言问题转为数据库查询，终端模块则承担命令执行与任务编排。这样一来，前端并不只是“把结果显示出来”，而是承担了真实的交互入口。

在系统测试方面，本文设计并执行了十组场景化用例，覆盖采集、清洗、分析、入库、可视化与安全等关键环节。测试结果表明，系统在功能完整性、数据一致性与运行稳定性方面基本达到了预期目标。

7.2 创新点归纳

围绕土地流转新闻文本的自动化处理与智能分析，本文主要形成了以下几方面具有一定特色的实现思路。

第一，本文没有把采集、分析和应用拆成彼此孤立的实验模块，而是把数据抓取、语义加工、数据库存储和桌面端展示连成了一条完整链路。两端通过 MySQL 解耦协作，后续扩展新的数据源或应用功能时更容易落地。

第二，在 Text-to-SQL 模块中，系统并没有把模型输出直接送去执行，而是增加了多层安全校验，包括危险语句拦截、白名单表控制、时间范围约束和结果上限控制。同时，界面会把实际执行的 SQL 一并展示给用户，便于核验查询过程，这种“可追溯”设计提升了问答结果的可信度。

第三，LLM 分析部分同时保留了同步和异步两种运行模式。前者便于调试，后者更适合批量处理；在异步模式下，再结合 `ijson` 的流式读写，能够明显缩短大批量新闻分析所需时间。

第四，应用端在结构设计上尽量保持轻量，没有额外引入复杂的路由层，而是以较直接的页面切换方式配合集中式 IPC 注册完成主要业务逻辑。这种实现不算复杂，但在当前项目规模下更容易维护和排错。

7.3 不足与未来研究方向

虽然本文已经完成了系统的核心功能实现，但受研究周期、数据来源和硬件条件等因素限制，当前方案仍存在一些不足，后续还有进一步完善的空间。

第一，数据源仍然偏少。目前系统主要接入了三个核心新闻网站，还没有覆盖微信公众号、政府公告页面、学术数据库以及短视频平台文本等更多渠道。后续可以继续扩展 Spider 类型，或结合 API 对接方式，逐步扩大数据覆盖面。

第二，地区维度的分析能力还不够。现阶段系统主要能做来源统计，但还没有把省、市、县等行政区划信息稳定提取出来。后续若能在清洗阶段加入地名实体识别，就有机会把地区对比、地区热度和地区关键词差异等分析做得更细。

第三，时间序列预警机制尚未加入。当前看板能够展示月度趋势，但更多还是“看得见”，还做不到“自动提醒”。后续如果引入移动平均、突变点检测等方法，就可以进一步把异常波动识别出来，提升舆情监测的主动性。

第四，大模型目前仍完全依赖云端 API。虽然通过截断正文、关闭深度推理和异步并发等方式已经对成本做了控制，但如果后续进入更高频使用场景，费用、网络延迟和隐私风险都仍然值得关注。下一步可以尝试基于 Ollama、vLLM 等框架部署本地模型，提升可控性。

第五，数据质量评估机制还比较粗。当前更多依赖人工抽检和场景化测试，缺少统一、自动化的质量评估方案。后续若能建立标注样本集，并把字段提取准确率和情感分析一致性纳入持续评估，就能更系统地观察模型和规则的表现变化。

第六，多图表联动尚未实现。当前看板中的各组件仍以独立展示为主，例如点击饼图中的某个情感区间时，还不能同步驱动趋势图或词云一起过滤。后续若结合 ECharts 的 `dispatchAction` 和 Vue 响应式机制，可以把多图联动能力补上，让用户进行更灵活的钻取分析。

总体来看，本文构建的农村土地流转新闻文本采集与分析系统，已经能够完成从网页抓取到交互应用的基本闭环。虽然系统仍有不少可以继续完善的地方，但它至少证明了一件事：围绕农业政策新闻构建一套可持续更新、可分析、可交互的数字化处理工具，在工程上是可行的。随着数据源继续扩展、分析方法继续细化，以及本地化部署逐步推进，该系统仍有较大的改进空间，也有望为政策研究和管理决策提供更直接的数据支持。

参考文献

[1] 中共中央, 国务院. 关于全面推进乡村振兴加快农业农村现代化的意见[Z]. 2021.
[2] 秦亚红, 普措才仁. 基于Scrapy的新闻网页数据抓取设计[J]. 电子技术与软件工程, 2020(4): 170-171.
[3] 刘多林, 吕苗. Scrapy框架下分布式网络爬虫数据采集算法仿真[J]. 计算机仿真, 2023, 40(6): 504-508.
[4] 石凤贵. 基于jieba中文分词的中文文本语料预处理模块实现[J]. 电脑知识与技术, 2020, 16(14): 248-251.
[5] 王鹤, 陆亦王, 刘夏璞, 等. 大语言模型驱动的期货市场新闻多主题和多层次情感分析框架[J]. 北京师范大学学报(自然科学版), 2025, 61(6): 751-757.
[6] 刘喜平, 舒晴, 何佳壕, 等. 基于自然语言的数据库查询生成研究综述[J]. 软件学报, 2022, 33(11): 4107-4136.
[7] 高雨, 罗帆, 张静, 等. 基于Electron框架可跨平台的数据采集桌面应用程序的开发及构建方法[J]. 新一代信息技术, 2025, 8(3): 24-31.
[8] 银笛. ECharts类库在气象数据可视化图形绘制中的应用[J]. 电子世界, 2021(22): 182-183.
[9] 陈灏慷, 陈文吟, 王德钦, 等. 基于文本挖掘的政务新媒体发展现状舆情分析[J]. 传媒论坛, 2020, 3(17): 21-22.
[10] 周志华. 机器学习[M]. 北京: 清华大学出版社, 2016.
[11] (美) 马瑟斯 (Matthes, E.). Python编程: 从入门到实践[M]. 袁国忠, 译. 北京: 人民邮电出版社, 2016.
[12] (美) 克罗克 (Kroenke, D. M.), (美) 奥厄尔 (Auer, D. J.). 数据库原理[M]. 赵艳, 等译. 5版. 北京: 清华大学出版社, 2011.
[13] 霍春阳. Vue.js设计与实现[M]. 北京: 人民邮电出版社, 2022.
[14] 何晗. 自然语言处理入门[M]. 北京: 人民邮电出版社, 2019.
[15] Scrapy Developers. Scrapy 2.11 Documentation[EB/OL]. https://docs.scrapy.org, 2024.
[16] Jieba Developers. Jieba 中文分词[EB/OL]. https://github.com/fxsjy/jieba, 2024.
[17] Alibaba Cloud. DashScope API Reference[EB/OL]. https://www.aliyun.com/product/bailian, 2025.
[18] MySQL Documentation. MySQL 8.0 Reference Manual[EB/OL]. https://dev.mysql.com/doc/refman/8.0, 2024.
[19] Electron Community. Electron Documentation[EB/OL]. https://www.electronjs.org/docs, 2025.
[20] Apache ECharts. ECharts 5 Documentation[EB/OL]. https://echarts.apache.org, 2024.
[21] Deng K, Chen S, Deng J. On optimisation of web crawler system on Scrapy framework[J]. International Journal of Wireless and Mobile Computing, 2020, 18(4): 332-338.
[22] Alymkulov D. Desktop Application Development Using Electron Framework: Native vs. Cross-Platform[J]. 2019.
[23] Liu X, Shen S, Li B, et al. A Survey of NL2SQL with Large Language Models: Where are we, and where are we going[J]. arXiv preprint arXiv:2408.05109, 2024.

致 谢

在本论文即将完成之际，谨向所有在毕业设计过程中给予帮助的人致以诚挚的谢意。

首先，感谢指导教师陈林凯老师在选题、技术方案设计与论文撰写过程中的悉心指导。陈老师在系统架构设计、技术选型论证与论文表达规范方面提出了许多宝贵意见，确保了本研究的学术严谨性与工程可行性。

感谢江苏理工学院计算机工程学院各位老师在专业课程学习中传授的知识与技能。为本文的工程实践提供了扎实的理论基础。感谢同学们的交流讨论与技术支持，特别是在 Electron 桌面应用开发与 Python 分布式爬虫调试过程中给予的帮助。

感谢开源社区与阿里云 DashScope所提供的优质技术资源，使得本文能够在成熟的技术生态基础上顺利开展研发与实验工作。

最后，感谢家人在学习期间的持续支持与理解。

附 录

附录 A：核心代码文件清单

本系统数据生产端（land-news-engine）的核心代码文件目录如下。

- crawler/spiders/agri_info_scrapy_spider.py：农业农村信息网 Scrapy 爬虫，实现 start_requests → parse_list → parse_detail 回调链
- crawler/spiders/moa_scrapy_spider.py：农业农村部 Scrapy 爬虫，基于 FormRequest 的 POST 搜索适配
- crawler/spiders/rmrb_scrapy_spider.py：人民日报/中国日报 Scrapy 爬虫
- crawler/pipelines.py：Scrapy Item Pipeline 定义（LandNewsPipeline、DatabasePipeline）
- crawler/middlewares.py：Scrapy 下载中间件定义（RandomUserAgentMiddleware、ProxyMiddleware）
- processor/cleaner.py：DataCleaner 类：DOM 解析、XPath 降级提取、文本规范化）
- processor/analyzer.py：LlmAnalyzer 类：DashScope SDK / REST 三层容错调用、BatchRunner 同步模式、AsyncBatchRunner 异步并发模式）
- processor/extractor.py：精确分词、停用词过滤、Counter 全局计数）
- processor/persist.py：去重合并、字段标准化、MySQL 批量入库）
- storage/db_client.py：PyMySQL + DictCursor、executemany 批量插入）
- config/settings.py：MySQL 连接参数、爬虫延迟、情感阈值）
- config/agri_settings.py / moa_settings.py / rmrb_settings.py：Cookies、Headers、关键词）

本系统数据应用端（terminal-helper / DumbOx）的核心代码文件目录如下。

- src/main/index.js：创建无边框窗口、注册 IPC 处理器
- src/main/ipcHandlers.js：数据持久化、MySQL 查询、Qwen API 调用、Shell 执行、PTY 终端管理）
- src/main/store.js：%APPDATA%/terminal-helper/data/）
- src/preload/index.js：加载脚本，暴露 terminalHelper API
- src/renderer/App.vue：管理全部页面的状态、ECharts 实例与数据流
- src/renderer/modules/board/BoardView.vue：情感饼图、趋势线、来源柱状图、情绪词云）
- src/renderer/modules/qa/QaView.vue：六格决策卡片 + 可追溯性面板）
- src/renderer/modules/library/LibraryView.vue：分页、搜索、详情展开）
- src/renderer/modules/cards/CardsView.vue：创建、编辑、拖拽排序、运行）
- src/renderer/modules/collections/CollectionsView.vue：
- src/renderer/modules/tasks/TasksView.vue：进度条、完成徽章、历史日志）
- src/renderer/modules/terminal/TerminalView.vue：标签终端
- src/renderer/modules/terminal/terminalStore.js：cards、collections、tasks、settings）
- src/renderer/modules/settings/SettingsView.vue：Shell 路径、Qwen API Key/Base URL/Model）
- src/renderer/composables/useCollectionRunner.js：
- src/renderer/composables/useTaskRunner.js：
- src/renderer/composables/useTerminalRuntime.js：

附录 B：Qwen 情感分析调用提示词清单

以下为本系统在情感分析环节使用的核心提示词模板。

System Message：
"你是一个只返回 JSON 格式数据的助手。"

User Message（情感分析）：
"你是一个农村土地政策研究专家。请分析以下新闻：\n标题：{title}\n正文：{trimmed_content}\n\n任务要求：\n1. 情感评分：0.0（极负面）到 1.0（极正面），中性为 0.5。\n2. 关键词：提取 3 个与土地流转相关的专业词汇。\n3. 必须只输出 JSON 格式，严禁包含任何解释文字。\n格式示例：{\"score\": 0.85, \"keywords\": [\"承包权\", \"经营权\", \"流转费\"]}"

附录 C：Text-to-SQL 系统提示词清单

以下为本系统在智能问答模块使用的核心提示词模板（详见 termal-helper/qwen_prompt.md）。

SQL 生成阶段 System Prompt：声明可访问的表（land_news_analysis、word_frequency_stats）及字段中文语义。约束仅生成 SELECT 语句，禁止 UPDATE/DELETE/INSERT/DROP 等危险操作，要求对决策咨询类问题转化为数据查询后回答，仅当完全无法映射时返回兜底提示。

SQL 摘要阶段 System Prompt：要求基于查询结果按"结论/证据/建议动作/风险等级/置信度"五段模板输出结构化中文摘要，并附带可追溯 SQL 与数据时间范围。

附录 D：MySQL 数据表结构 DDL

核心表 DDL 如下。

```sql
CREATE DATABASE IF NOT EXISTS land_news CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS land_news_analysis (
    id INT AUTO_INCREMENT PRIMARY KEY,
    url VARCHAR(500),
    title VARCHAR(500),
    publish_date DATE,
    source VARCHAR(100),
    content_summary TEXT,
    sentiment_score DECIMAL(4,3),
    keywords JSON,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) CHARACTER SET utf8mb4;

CREATE TABLE IF NOT EXISTS word_frequency_stats (
    id INT AUTO_INCREMENT PRIMARY KEY,
    word VARCHAR(200) NOT NULL,
    count INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) CHARACTER SET utf8mb4;
```
